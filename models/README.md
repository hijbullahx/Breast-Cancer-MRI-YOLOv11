# Models Directory

This folder stores trained YOLOv11 models.

**Note:** The actual `.pt` model files are ignored by Git because they are too large. 
After training in Colab, download `best.pt` and place it here locally if you want to run inference on your own machine.